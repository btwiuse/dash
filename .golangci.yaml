linters:
  disable-all: true
  enable:
    - gofumpt
    - staticcheck
    - govet
    - gci

linters-settings:
  gci:
    sections:
      - standard
      - prefix(github.com/btwiuse/dash)
      - default
  staticcheck:
    go: '1.18'
